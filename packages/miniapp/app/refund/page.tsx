// /refund — submit refund(dealHash) after deadline if MM never locked (SPEC §6.2).

export default function RefundFlow() {
  return (
    <main style={{ padding: 24, fontFamily: "system-ui" }}>
      <h1>Refund</h1>
      <p>TODO: writeContract(refund, [dealHash]).</p>
    </main>
  );
}
