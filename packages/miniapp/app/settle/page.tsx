// /settle — submit settle(dealHash) after both lock events observed (SPEC §6.2).

export default function SettleFlow() {
  return (
    <main style={{ padding: 24, fontFamily: "system-ui" }}>
      <h1>Settle trade</h1>
      <p>TODO: writeContract(settle, [dealHash]).</p>
    </main>
  );
}
